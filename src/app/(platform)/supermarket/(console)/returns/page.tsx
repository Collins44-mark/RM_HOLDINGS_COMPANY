import { ReturnsManager } from "@/components/supermarket/ReturnsManager";

export const metadata = { title: "Returns" };

export default function SupermarketReturnsPage() {
  return <ReturnsManager />;
}
